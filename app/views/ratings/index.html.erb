<h2>List of ratings</h2>

<p>Total Ratings Given: <%= @ratings.count %>
<ul>
 <% @ratings.each do |rating| %>
   <li><%= link_to rating.beer.name, rating.beer %> <%= rating.score %><%= button_to 'delete', rating, method: :delete, form: { data: { turbo_confirm: 'Are you sure?' } } %></li>
 <% end %>
</ul>

 <button>
  <%= link_to "Create New Rating", new_rating_path %>
</button>
